{
  "name": "stardew-todo-worker",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Shared task mirror, comments, and reminder Worker for 今日任务 · 像素田园待办",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "node --check src/index.js && node --check src/db.js && node --check src/push.js && node --check src/validation.js",
    "lint": "npm run check",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@mmmike/web-push": "1.3.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-plugin": "1.0.0",
    "@cloudflare/workers-types": "5.20260823.1",
    "vitest": "4.1.11",
    "wrangler": "4.125.0"
  }
}
