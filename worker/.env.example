# Set these with `wrangler secret put`, never commit their values.
VAPID_SUBJECT=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# Optional comma-separated exact origins. The Wrangler config includes the
# production GitHub Pages origin and localhost development origins.
ALLOWED_ORIGINS=
