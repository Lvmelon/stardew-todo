name: Deploy Cloudflare Worker

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - worker/**
      - .github/workflows/deploy-worker.yml

permissions:
  contents: read

concurrency:
  group: cloudflare-worker-production
  cancel-in-progress: false

env:
  # Account IDs are public routing identifiers, not credentials. Keeping the
  # verified value in versioned configuration avoids whitespace/copy errors in
  # an unnecessary secret while the API token remains encrypted.
  CLOUDFLARE_ACCOUNT_ID: bb1a9cbde1e56c8e97ca57bec46141ee

jobs:
  deploy:
    name: Apply D1 migrations and deploy Worker
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: worker/package-lock.json

      - name: Install Worker dependencies
        run: npm ci
        working-directory: worker

      - name: Verify Worker before production deployment
        run: npm run check && npm test && npm run deploy:dry-run
        working-directory: worker

      - name: Apply remote D1 migrations
        working-directory: worker
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: npx wrangler d1 migrations apply stardew-todo --remote --config wrangler.jsonc

      - name: Deploy Worker
        id: deploy
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ env.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: worker
          command: deploy

      - name: Verify deployed Worker health
        env:
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment-url }}
        shell: bash
        run: |
          test -n "$DEPLOYMENT_URL"
          curl --fail --silent --show-error --retry 5 --retry-delay 5 "${DEPLOYMENT_URL%/}/health"
