# AGENTS.md

本文件适用于仓库根目录及其全部子目录。所有参与本项目的开发者和自动化代理都必须遵守。

## 1. 项目定位

- 项目名称：`今日任务 · 像素田园待办`。
- 线上地址：<https://lvmelon.github.io/stardew-todo/>。
- 这是给项目所有者女朋友使用的轻量级待办 PWA。
- 核心理念：治愈待办、简单、漂亮、没有压力，并带一点星露谷式的游戏感。
- 用户打开应用后应愿意完成任务，并像查看小镇公告板一样自然地查看待办。
- 本项目不是复杂的任务管理系统，也不是重度游戏化产品。

## 2. 技术与部署边界

- 前端继续保持静态 PWA：HTML、CSS、Vanilla JavaScript、IndexedDB、Service Worker、Web App Manifest；不得无理由引入 React、Vue 或大型构建系统。
- V1.0 可以增加独立的 Cloudflare Worker + D1 后端，但它只提供共享任务镜像、留言、配对、设备 Push 订阅、健康检查和 Cron 提醒 API。GitHub Pages 仍只发布静态前端。
- 任务主数据优先保存在使用者本机 IndexedDB，失败时降级到 localStorage，再失败时使用内存。D1 不是本地任务的完整云端主库。
- Service Worker 提供离线应用壳和 Web Push 事件处理；`manifest.webmanifest` 与 Apple 主屏幕元数据提供 PWA 安装能力。
- `main` 是 GitHub Pages 发布分支，站点按项目子路径 `/stardew-todo/` 部署。新增或修改资源路径时必须继续兼容该子路径。
- 不得把本地持久化或 D1 共享镜像描述成完整多设备同步、云备份或无条件恢复。清除站点数据、卸载并清理网站数据或更换设备时，本地主数据可能无法恢复；完整迁移使用 JSON 导出/导入。

## 3. 视觉基准与版权边界

- `assets/scene.webp` 是已经确认的核心视觉基准，也是后续 UI 调整的视觉锚点。
- 除非项目所有者明确要求重构视觉，否则不得重新设计整体布局，不得替换主场景，也不得改变公告板在场景中的核心位置。
- 后续 UI 必须保持以下特征：
  - 像素田园、Stardew Valley inspired 的乡村氛围；
  - 蓝天、草地、农舍、木栅栏、小路、花草等场景元素；
  - 中央大型木质任务公告板与羊皮纸任务区域；
  - 两个像素情侣角色；
  - 木质 UI 与类似原版星露谷的暖色调气质。
- 新增覆盖层、弹窗、按钮或设置页时，应沿用现有木质、羊皮纸、暖色和像素化语言，并检查其与主图的定位关系。
- 这是风格启发，不是素材复刻。不得提取、复制或重新分发 Stardew Valley 的原始 sprite、字体、音乐、音效或其他受版权保护素材。
- 未经明确确认，不得为了“现代化”“统一组件”或“提高视觉效果”而改变已经确认的视觉基准。

## 4. 产品原则

用户希望：

- 打开后就想完成任务；
- 简洁、治愈、漂亮、低压力；
- 有轻量游戏感；
- 像查看小镇公告板一样查看任务。

用户不希望：

- 复杂流程或大量分类；
- 强游戏化；
- 连续打卡压力；
- 积分、奖励商城；
- 每日结算；
- 农场经营系统。

判断新功能时，优先问它是否降低操作负担、增强完成意愿并保持治愈感。不要因为技术上可行就扩大产品范围。

## 5. 当前能力基线与 V1.0 目标

当前已有能力：

- 查看今日任务；
- 新建、编辑、删除和完成任务；
- 完成后任务自动从开放任务列表消失，并提供轻量反馈；
- 任务描述、emoji 和可选截止日期；
- 超过首页展示上限时进入全部任务列表；
- IndexedDB 本地存储、localStorage 降级和内存兜底；
- Service Worker 离线缓存；
- PWA manifest；
- iPhone 添加到主屏幕；
- GitHub Pages 发布。

不要把尚未由代码、测试和线上响应分别证实的能力写成现有功能。V1.0 目标包括：真实的今日/逾期语义、Web Push、设置页、共享任务镜像、任务留言、情侣空间、JSON 导入导出、本地备份恢复、PWA 更新提示、天气、时间/季节/天气氛围、原创 procedural BGM、轻量植物成长、正式测试、CI 与 Worker 自动部署。

V1.0 明确不做：完整 Todo 云同步、双向多设备编辑、outbox 分布式队列、LWW 冲突解决、伙伴修改对方任务、全量设置云同步、传统账号体系、聊天 App、连续签到、积分奖励商城和农场经营系统。

## 6. 现有文件职责

- `index.html`：应用结构、任务详情/编辑/删除/全部任务弹窗和 PWA 元数据入口。
- `styles.css`：主场景覆盖布局、公告板任务区、木质与羊皮纸 UI、动效和响应式规则。
- `app.js`：DOM 绑定、渲染、交互流程、默认任务、日期问候和 Service Worker 注册。
- `task-model.js`：任务校验、创建、更新、完成、软删除和首页可见任务计算。
- `storage.js`：IndexedDB、localStorage、内存存储与运行时降级。
- `sw.js`：应用壳缓存、旧缓存清理和 GET 请求离线策略。
- `manifest.webmanifest`：PWA 名称、范围、启动路径、显示方式、主题色和图标。
- `assets/scene.webp`：已确认的核心场景图，不得随意替换或重绘。
- `assets/parchment-tile.png`：羊皮纸纹理。
- `icons/`：PWA 与 Apple 主屏幕图标。
- `worker/`：Cloudflare Worker 路由、D1 migrations、Cron 和 Web Push；它不能取代本地任务主数据。
- `PRD.md`、`ARCHITECTURE.md`、`SECURITY.md`、`DEPLOYMENT.md`、`CREDITS.md`：V1.0 产品、架构、安全、部署和素材边界；描述必须与代码和验收状态一致。
- `README.md`、`GITHUB-PAGES-DEPLOY.md`：能力说明与部署说明；修改功能或发布流程时同步检查文档是否仍然准确。

## 7. 事实与证据规范

- 回答或实施前先检查需求中是否存在错误前提、逻辑跳跃、概念混淆、信息缺失或未经证实的假设；发现时直接指出。
- 独立判断，不自动认同项目所有者或既有文档中的结论。
- 明确区分：已由代码、测试或运行结果核实的事实；基于现有信息的合理推测；主观设计判断；暂时无法证实的信息。
- 涉及数字、日期、人物、引用、研究结论、案例或具体事件时尽可能核实来源；不能确认时说明不确定性，不得编造、补全或伪装成事实。
- 区分“证据不足”和“结论错误”。缺少证据不等于结论必然错误，可能成立也不等于已经证实。
- 当不同意某项判断时，清楚说明不同意的部分、原因、关键证据、潜在风险和更合理的替代解释。
- 主动指出被忽略的适用条件、重要变量、隐含风险、矛盾和偏差，但不要借机扩大实现范围。
- 文档描述、代码实现与线上行为不一致时，以可复现的代码和运行证据为准，并明确记录差异。

## 8. 修改前的必做工作

每次修改前必须：

1. 阅读与需求相关的现有代码、样式、资源、文档和近期 Git 变更；
2. 判断影响范围，包括数据模型、持久化、离线缓存、PWA 安装、GitHub Pages 子路径、移动端布局和既有视觉；
3. 先给出实施计划，明确修改文件、保持不变的内容、测试方式和验证标准；
4. 对会显著改变产品范围、视觉基准、数据格式、权限、通知或同步方式的决策，先获得项目所有者确认。

不要顺手重构无关代码，不要把一个小需求扩成架构迁移。

## 9. 测试与验证纪律

- 功能或 Bug 修改应优先先写能够复现目标行为的测试，再写最小实现。
- 保持测试轻量并符合 Vanilla JavaScript 架构；没有充分理由时，不要只为测试引入大型工具链。
- 修改后至少执行：
  1. 相关自动化测试；
  2. JavaScript 语法或模块检查；
  3. `git diff --check`；
  4. 完整 `git diff` 审查；
  5. 浏览器中的实际功能验证。
- 涉及存储时，验证 IndexedDB 正常路径、localStorage 降级路径以及刷新后的数据结果。
- 涉及 Service Worker 时，验证首次加载、离线应用壳、旧缓存升级和 GitHub Pages 子路径，不得只修改缓存名后宣称更新完成。
- 每次发布任何前端静态资源时都必须修改 `sw.js` 并递增缓存版本；否则已安装 PWA 可能先读取旧缓存，且不会进入可提示的新 Service Worker 生命周期。
- 涉及 PWA 更新时，区分“新 Service Worker 已安装”“当前页面已由新版本控制”和“用户已经看到更新提示”。
- 涉及提醒 / 通知时，验证权限拒绝、权限撤销、平台限制、应用在前台/后台/关闭时的差异；不得把浏览器通知能力误写成可靠的服务器定时推送。
- 涉及共享镜像时，验证本地先写、Worker 失败不阻塞本地 Todo、`pendingShareSync` 在打开/联网/手动刷新时的轻量重试、伙伴只读权限、留言 append-only、删除 tombstone 和 D1 索引查询；不要引入或声称 outbox/LWW/双向同步。
- 涉及配对与安全时，验证 URL fragment 读取后清理、secret/recovery code 不进入 query/日志/分析、服务器只保存 hash、Bearer 只走 HTTPS、owner/partner 权限和撤销流程。
- 涉及 Cloudflare 时，分别验证 Worker 健康检查、D1 migration、CORS、Cron、VAPID secret 和 GitHub Actions；没有线上响应或控制台证据时，不得声称后端已部署。
- 涉及视觉时，在桌面和窄屏/手机尺寸渲染检查主场景、覆盖层对齐、弹窗、文字溢出、安全区和可点击区域。
- 验证以实际输出、DOM、存储结果、网络响应或渲染截图为证据，不能只依据命令退出码。

## 10. Git 与发布规则

- 开始修改前检查 `git status`，保留并避开项目所有者已有的未提交改动。
- 修改完成后检查完整 diff，确认没有意外文件、调试代码、临时资源、版权素材或无关格式化。
- 只有在相关测试和功能验证通过后才可以 commit；commit 应聚焦当前需求并使用清晰信息。
- 不得因为“修改完成”就自动发布。
- 只有项目所有者明确要求“发布”或“推送”后，才可执行 `git push origin main`。
- push 后应核验 GitHub Pages 已更新到目标提交，并区分“push 成功”“Pages 构建完成”“线上资源已更新”三个状态。
- Cloudflare Worker 的部署必须由明确的 CI/手动发布流程完成，并分别核验 Worker、D1、Cron 和 Pages；未经明确要求，不修改远端设置、Pages 配置、仓库可见性或历史提交。

## 11. 默认决策顺序

当需求存在多种实现方式时，按以下顺序取舍：

1. 不破坏现有数据；
2. 不改变已确认视觉基准；
3. 保持简单、治愈、低压力；
4. 保持静态 PWA 和 GitHub Pages 兼容；
5. 优先小而清晰、可测试、可回退的实现；
6. 最后再考虑增加抽象、依赖或游戏化元素。
